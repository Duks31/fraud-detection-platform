project: sentinel_fraud_detection
provider: local

registry:
  registry_type: sql
  path: postgresql+psycopg://sentinel_user:sentinel_secure_pass@localhost:5432/feast_registry
  cache_ttl_seconds: 60

online_store:
  type: redis
  connection_string: "localhost:6379"

offline_store:
  type: file

